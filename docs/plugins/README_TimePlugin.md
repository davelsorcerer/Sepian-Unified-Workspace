# TimePlugin
The TimePlugin provides functionality for getting the current time in various formats.

### Commands
* `now`: Gets the current time in various formats.

### Usage
To use the TimePlugin, simply call `now`. The plugin will return the current time in a format suitable for display. For example:
* To get the current date and time, call `now`.